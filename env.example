# Порог для удаления похожих предложений (дубликатов)
# Если евклидово расстояние между предложениями < E1, они считаются похожими
E1=0.75

# Количество предложений, которые должны остаться после этапа 2
# Оставляются предложения с наименьшим расстоянием до запроса
# Если после этапа 1 осталось меньше предложений, все остаются
E2=15

# URL эндпоинта для embedding модели (по умолчанию ollama)
EMB_URL=http://localhost:11434

# Название модели для embeddings
# Для ollama используйте формат: ollama/<model-name>
# Например: ollama/nomic-embed-text
EMB_MODEL=ollama/nomic-embed-text

# Размер батча для получения embeddings
BATCH_SIZE=64

